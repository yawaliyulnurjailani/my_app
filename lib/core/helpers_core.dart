class HelpersCore {
  // static double screenPercent(BuildContext context, double size) {
  //   double result = 0;
  //   if (maxWidth(context) <= 500) {
  //     result = (size / 500) * 500;
  //   } else if (maxWidth(context) > 500 && maxWidth(context) <= 700) {
  //     result = (size / 500) * maxWidth(context);
  //   } else if (maxWidth(context) > 700 && maxWidth(context) <= 900) {
  //     result = (size / 700) * maxWidth(context);
  //   } else if (maxWidth(context) > 900 && maxWidth(context) <= 1100) {
  //     result = (size / 900) * maxWidth(context);
  //   } else if (maxWidth(context) > 1100 && maxWidth(context) <= 1300) {
  //     result = (size / 1100) * maxWidth(context);
  //   } else if (maxWidth(context) > 1300 && maxWidth(context) <= 1500) {
  //     result = (size / 1300) * maxWidth(context);
  //   } else if (maxWidth(context) > 1500) {
  //     result = (size / 1500) * 1920;
  //   }
  //   return result;
  // }
}
